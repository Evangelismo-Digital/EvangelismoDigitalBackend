import { SHARED_PROVIDER_DEFAULTS } from './shared'

export const VIACEP_CONFIG = {
  TIMEOUT_MS: 3000,
  MAX_RETRIES: 2,
  BACKOFF_MS: 200,
  HTTPS_AGENT: SHARED_PROVIDER_DEFAULTS.HTTPS_AGENT,
} as const
