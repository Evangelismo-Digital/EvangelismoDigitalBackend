node_modules
coverage
build
/src/generated/prisma
.env
.ci-local

# Mutation testing (Layer 5)
/reports/mutation
/.stryker-tmp

# Claude Code: configuration is global-only (~/.claude/settings.json).
# No project-level settings file may exist — it would override the global one.
.claude/settings.json
.claude/settings.local.json
# SonarQube local state (admin password + analysis token, 0600)
.sonar/

# Generated analysis / security reports
/reports/sonar
/reports/eslint
/reports/security

# Local security scanner credentials (SNYK_TOKEN), 0600
.env.security
